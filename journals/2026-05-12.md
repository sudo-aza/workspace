# 2026-05-12 (12:00 maintenance run)

## latex-1: Portable LuaLaTeX Installation

VM was reset — had to re-clone both repos.

Installed TeXLive 2026 portable to `/home/z/.texlive/2026/` using scheme-basic.
Wrote install script at `scripts/install-texlive.sh` (idempotent, handles VM resets).

### Key details
- Installer: `install-tl --profile` with scheme-basic
- Binary: `/home/z/.texlive/2026/bin/x86_64-linux/lualatex`
- Version: LuaHBTeX 1.24.0 (TeX Live 2026)
- Packages installed: pgf, fontspec, geometry, titlesec, tocloft, booktabs, enumitem, xcolor, hyperref, microtype, koma-script, calc, etoolbox, listings, caption, luatexbase, luaotfload, metalogo, mdframed, tcolorbox, parskip, setspace, fancyhdr, lastpage, bookmark, amsmath, amssymb, mathtools, unicode-math, fontawesome5, sourcecodepro
- Verified with test document (math, table, tikz) — compiles successfully
- PDF→PNG via `pdftoppm` (already on system)

### Notes
- `graphicx` and some packages are part of scheme-basic — tlmgr errors on them but they work
- `sourcesanspro` font package had issues — may need OS fonts as fallback
- Profile key `tlpdotdir` is not valid in TeXLive 2026 — removed it
- Need `export PATH="/home/z/.texlive/2026/bin/x86_64-linux:$PATH"` before any latex command

# 2026-05-12 (18:00 maintenance run)

## latex-2: Beautiful Title Page Design

VM was reset again — re-cloned repos, re-ran install-texlive.sh (idempotent, worked perfectly).

### Design
Created two title page variants using TikZ overlays, inspired by the Typst Thesis Starter aesthetic:

**Dark theme (Page 1):**
- Full navy (#1A1A2E) background with left accent stripe
- White bold title (28pt), light subtitle (14pt), right-aligned
- Thick blue rule separator
- Author, institution, date in lighter gray below
- Roman numeral page number

**Light theme (Page 2):**
- White background with dark top accent bar
- Vertical navy bar on left as visual anchor
- Bold title (30pt), navy subtitle, left-aligned
- Thin separator, author block with institution details
- Large faded watermark letter in bottom-right corner

### VLM Verification
- Both pages rated **8/10** by VLM
- Text correctly visible after fixing compilation (needed 2 passes for TikZ `remember picture`)
- Initial `\IfNoValueTF` usage was broken (missing xparse) — rewrote without it
- Key lesson: always compile TikZ overlay documents TWICE

### Files
- `scripts/latex/sudodoc-titlepage.tex` — the source
- `scripts/latex/output/dark.png` — dark theme preview
- `scripts/latex/output/light.png` — light theme preview
